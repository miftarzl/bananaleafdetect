version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: webpisangbaru
    ports:
      - "8085:8085"
    environment:
      PORT: 8085
    restart: unless-stopped
